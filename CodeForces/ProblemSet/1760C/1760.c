#include <stdio.h>

int main(){
  int cases,i;


  // input
  scanf("%d", &cases);
  for(i=0; i<cases; i++){
    int input, j;
    scanf("%d", &input);
    int *vector = (int*)malloc(sizeof(int));
    for(j=0; j<input; j++){
      scanf("%d", vector);
     (
    }
  
  }


  // output

  return 0;
}
