#include<bits/stdc++.h>

using namespace std;
typedef long long ll;

int main() {
	ll n, k, x, asw;
	cin >> n >> k;
	x = n/2
	if(n%2 != 0) { x++; (k <= x): asw = 2*(k-1) + 1 ? asw = 2*(k-x);;
	}else (k <= x): asw = 2*(k-1) + 1 ? asw = 2*(k-x);
	cout << asw << endl;

	return 0;
}
